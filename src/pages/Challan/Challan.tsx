import React from 'react';
import Breadcrumb from '../../components/Breadcrumbs/Breadcrumb';
import DefaultLayout from '../../layout/DefaultLayout';

const Challan: React.FC = () => {
    return (
        <DefaultLayout>
            <Breadcrumb pageName="Challan" />
        </DefaultLayout>
    );
};

export default Challan;
